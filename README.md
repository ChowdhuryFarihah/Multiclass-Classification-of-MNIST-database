# Multiclass-Classification-of-MNIST-database
Using CNNs to classify digits within the MNIST database of 60,000 training images and 10,000 test images.

Utilized the following libraries:
* PyTorch
* Numpy
* MatPlotLib
